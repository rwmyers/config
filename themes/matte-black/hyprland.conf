general {
    col.active_border = rgb(8A8A8D)
}

windowrule {
    name = fullscreen-border
    match:fullscreen = true

    border_color = rgb(FF453A)
}

# Groupbar tabs match the waybar colors
$groupBarBackground = rgb(1e1e1e)
$groupBarText = rgb(8a8a8d)
