general {
    col.active_border = rgb(dcd7ba)
}

windowrule {
    name = fullscreen-border
    match:fullscreen = true

    border_color = rgb(c34043)
}

# Groupbar tabs match the waybar colors
$groupBarBackground = rgb(1f1f28)
$groupBarText = rgb(dcd7ba)
